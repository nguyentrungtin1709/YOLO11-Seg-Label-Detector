# Label Detector - Python Dependencies
# =====================================
# Sử dụng: pip install -r requirements.txt
# 
# Khuyến nghị: Tạo virtual environment trước khi cài đặt
#   python -m venv .venv
#   source .venv/bin/activate  # Linux/macOS
#   .venv\Scripts\activate     # Windows

# ========== GUI Framework ==========
# PySide6: Official Qt6 binding for Python
# - Hỗ trợ QVideoWidget, QCamera cho video display
# - Cross-platform: Windows, Linux, macOS
# - LGPL License
PySide6>=6.5.0

# ========== Computer Vision ==========
# OpenCV: Video capture và image processing
# - opencv-python-headless: Không bao gồm GUI (sử dụng Qt thay thế)
opencv-python-headless>=4.8.0

# ========== ML Inference ==========
# ONNX Runtime: High-performance inference engine
# - Hỗ trợ CPU/GPU acceleration
# - Nhẹ hơn PyTorch/TensorFlow
onnxruntime>=1.16.0

# ========== Numeric Computing ==========
# NumPy: Array processing
numpy>=1.24.0

# ========== Image Processing ==========
# Pillow: Image manipulation (backup cho việc lưu ảnh)
Pillow>=10.0.0

# ========== Document Preprocessing ==========
# PaddleOCR: Document orientation classification + OCR text extraction
# - Sử dụng cho việc phát hiện và sửa hướng tài liệu (0°, 90°, 180°, 270°)
# - OCR cho trích xuất text từ nhãn
paddleocr>=2.7.0

# PaddlePaddle: Deep learning framework for PaddleOCR
# - Backend cho PaddleOCR
# - CPU-only mode (không sử dụng GPU)
paddlepaddle>=2.5.0

# ========== QR Code Detection ==========
# zxing-cpp: High-performance barcode/QR code detection
# - C++ implementation with Python bindings (pybind11)
# - Supports QR, DataMatrix, Aztec, PDF417, etc.
# - No external dependencies required
zxing-cpp>=2.2.0

# ========== Optional: GPU Support ==========
# Nếu cần GPU acceleration, uncomment dòng dưới và comment onnxruntime ở trên
# onnxruntime-gpu>=1.16.0
