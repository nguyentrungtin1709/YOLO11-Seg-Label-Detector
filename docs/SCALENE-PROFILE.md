# Install Scalene
```bash
pip install scalene
```

# Using Scalene
```bash
scalene run ./main.py
```

# View Profile
After running your script with Scalene, it will generate a profile report in the terminal and save
```bash
scalene view scalene-profile.json
```